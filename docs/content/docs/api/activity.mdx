---
title: Activity
description: >-
  Report what your own client did — errors, timings, transactions — and read it
  back. Unlike the API logs, which only ever see requests that reached this
  service, these are the events that happen entirely on a device: a crash, a
  cancelled signature, a screen that failed before any request left it.
full: true
_openapi:
  preload:
    - ./openapi.json
  toc:
    - depth: 2
      title: Recent client events for this consumer, newest first
      url: '#recent-client-events-for-this-consumer-newest-first'
    - depth: 2
      title: Report a batch of client events (errors, metrics, transactions)
      url: '#report-a-batch-of-client-events-errors-metrics-transactions'
    - depth: 2
      title: Counts, top event types, top errors and a daily series
      url: '#counts-top-event-types-top-errors-and-a-daily-series'
  structuredData:
    headings:
      - content: Recent client events for this consumer, newest first
        id: recent-client-events-for-this-consumer-newest-first
      - content: Report a batch of client events (errors, metrics, transactions)
        id: report-a-batch-of-client-events-errors-metrics-transactions
      - content: Counts, top event types, top errors and a daily series
        id: counts-top-event-types-top-errors-and-a-daily-series
    contents:
      - content: >-
          `level` is a floor, not an exact match: `level=warn` returns warnings
          and errors.
        heading: recent-client-events-for-this-consumer-newest-first
      - content: >-
          Accepted, not created: the batch is written under the consumer the
          gateway authenticated, and nothing in the body can change that
          attribution. Over-long messages are truncated and an over-sized
          `props` is replaced with a marker rather than failing the call —
          losing a whole batch to one malformed field is worst exactly when the
          client is in a state it did not anticipate.
        heading: report-a-batch-of-client-events-errors-metrics-transactions
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi.json" webhooks={[]} operations={[{"path":"/v1/activity/events","method":"get"},{"path":"/v1/activity/events","method":"post"},{"path":"/v1/activity/summary","method":"get"}]} showTitle />
    </>
  );
}